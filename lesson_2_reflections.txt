What happens when you initialize a repository? Why do you need to do it?

Initializing my repository sets up my git for the first time.  This is so git knows what is being commited to one project.

How is the staging area different from the working directory and the repository? What value do you think it offers?

The staging area add another layer of confirmation and control over what exactly you want to commit. The staging area is only a prelude to a commit.

How can you use the staging area to make sure you have one commit per logical change?

To see which files are going to be in the commit.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

Branches allow me to have different histories and therefore different possibilities such as implementing an easy mode into an asteroids game.

How do the diagrams help you visualize the branch structure?

It makes all branches obvious.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

I had a lot of difficulty merging the two, and I blame some inexplicable changes that were made to the coins branch after this exercise was created.  I probably learned things that I should've learned later on in this lesson.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

Git might mess up, but having it automated can also prevent human error.